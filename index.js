var gulpFactoryMongoError = require('./lib/gulp-factory-mongo-error');

module.exports = function(opts) {
//  return compose(uglify, logger)(opts);
};

module.exports.gulpFactoryMongoError = gulpFactoryMongoError;